<div id="main_body">
<h1>Position & Group</h1>
<table>
  <tr>
  	<th>#</th>
  	<th>UserGroup</th>
    <th>ModuleName</th>
    <th>ModuleMapping</th>
    <th></th> 
    <th></th> 
  </tr>

<% @sys_module_mappings.each do |p| %>
  <tr>
  	
    <td><%= p.id %></td> 
    <td><%= p.sys_user_group.user_group_name %></td> 	
    <td><%= p.sys_module.module_name %></td> 
    <td><%= p.sys_module.module_group_name %></td>
    <td><%= link_to 'Edit', edit_sys_module_mapping_path(p, :format => :js), :remote => true %></td>
    <td><%= link_to 'Delete', sys_module_mapping_path(p), :method => :delete, :confirm => 'Are you sure to delete?' %></td>
  </tr>
<% end %>

</table>

  <%= link_to "输入ModuleMapping", new_sys_module_mapping_path(:format => :js), :remote => true, :id => 'new_log_link' %>

</div>