<%= simple_form_for(@sys_user_position) do |f| %>

  <%= f.input :position_name, :label => '职位名称/Position Name:' %>
  <%= f.input :position, :label => '职位缩写/Position:', :hint => 'eng for engineering, acct for account' %>

  <%= f.button :submit, '保存' %>  
<% end %>
